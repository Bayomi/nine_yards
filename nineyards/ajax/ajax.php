

<?php
	
	/*$request_type = filter_input(INPUT_POST, "requestType", FILTER_SANITIZE_STRING);
	if ( empty( $request_type ) ) {
		echo 'Missing requestType.';
		die();
	}

	require_once "../config.php";
	$mysqli = new mysqli( DB_HOST, DB_USER, DB_PASSWORD, DB_NAME );

	if ($mysqli->errno) {
		print($mysqli->error);
		die();
	}

	*/



	//switch ( $request_type ) {
		//case "checkInformation":

		//Change the text/caption information
			
			// get the data -> filter_input(INPUT_POST, "firstName", FILTER_SANITIZE_STRING);
			
            //Build and execute the query
			
			/*$query = "INSERT INTO TEXT/CAPTIONS VALUES - Data;
			$result = $mysqli->query($query);
			
	
			//Return a message indicating the database was updated
			if ($result) {
				echo 'Update';
			} else {
				echo 'Fail';
				die();
			}

			break;
			*/
			
			
			




?>